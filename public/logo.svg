<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 64 64"
  role="img"
  aria-label="Slat favicon"
>
  <style>
    .text {
      font-family: 'Montserrat Alternates', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      font-weight: 700;
      font-size: 36px;
      fill: currentColor;
      text-anchor: middle;
      dominant-baseline: middle;
    }
    .bg {
      fill: #ffffff;
    }
  </style>

  <!-- Fondo redondeado -->
  <rect class="bg" width="64" height="64" rx="16" ry="16" />

  <!-- Texto centrado -->
  <text x="50%" y="50%" class="text">Slat</text>
</svg>
